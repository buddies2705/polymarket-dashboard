# Bitquery OAuth Token
# Get your token from: https://bitquery.io
# Required for fetching data from Bitquery API
BITQUERY_OAUTH_TOKEN=your_oauth_token_here

# Alternative variable name (legacy support)
# BITQUERY_API_KEY=your_oauth_token_here

# Bitquery API Endpoint (optional, defaults to streaming endpoint)
BITQUERY_ENDPOINT=https://streaming.bitquery.io/graphql

# Server Port (optional, defaults to 3001)
PORT=3001

# Database Path (optional, defaults to data/polymarket.db)
# For production, ensure this path is in a persistent volume
DB_PATH=data/polymarket.db
